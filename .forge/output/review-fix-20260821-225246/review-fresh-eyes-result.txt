Now let me read the full implementation files to understand the surrounding context:

Let me also check how `MSIDFlightManager.boolForKey:` behaves and the cached access-token code path:

I'll help you understand these three aspects of the codebase. Let me start by locating the relevant files.

Now let me read these files to answer all three questions:

Now let me search for the actual string values of these constants:

Perfect! Let me get the constants file to see all the relevant mappings:

Excellent! Now I have all the information needed. Let me provide comprehensive answers to all three questions:

## Summary of Findings

### 1. **How `MSIDFlightManager`'s `boolForKey:` works and what it returns when the key is absent**

**MSIDFlightManager.m (lines 138-153):**
- The `boolForKey:` method **initializes `result` to `NO`** at line 140
- It checks if a `flightProvider` is set (line 146)
- If a provider exists, it delegates to `[flightProvider boolForKey:flightKey]`
- **If no provider is set, or the key is not found, the method returns `NO`** (the default initialized value)

**MSIDFlightManagerMockProvider.m (lines 30-33):**
- The mock provider calls `[self.boolForKeyContainer[flightKey] boolValue]`
- **When a key is absent**, dictionary lookup returns `nil`, and calling `boolValue` on `nil` returns **`NO`**
- This confirms the same behavior: absent keys default to `NO`

**Key insight:** Both the real manager and the mock provider follow a consistent pattern — any undefined/absent flight key safely defaults to `NO` (boolean false).

---

### 2. **Sanitization in the cached access-token path of `MSIDBrowserNativeMessageGetTokenResponse.jsonDictionary`**

**Location:** MSIDBrowserNativeMessageGetTokenResponse.m, lines 179-208 (the `else` branch)

**Answer: NO, sanitization does NOT apply in the cached access-token path.**

- The `sanitizeResponse` boolean is set at line 130 based on the flight flag
- When `tokenResponse` is `nil` but `accessToken` is present (lines 179-208), the code manually constructs the response dictionary WITHOUT calling `MSIDBrowserNativeMessageGetTokenResponseSanitizedDictionary()`
- The fields are added directly:
  - Line 184: `response[@"access_token"]` 
  - Line 189: `response[@"token_type"]`
  - Line 194: `response[@"id_token"]`
  - Line 200: `response[@"scope"]`
  - Lines 205-206: `response[@"expires_on"]` and `response[@"expires_in"]`

**Critical difference:** The sanitization function (lines 47-67) only allows specific keys (`MSID_OAUTH2_ACCESS_TOKEN`, `MSID_OAUTH2_ID_TOKEN`, `MSID_OAUTH2_TOKEN_TYPE`, `MSID_OAUTH2_EXPIRES_IN`, `MSID_OAUTH2_EXPIRES_ON`, `MSID_OAUTH2_SCOPE`, `MSID_OAUTH2_REQUEST_CONFIRMATION`). In the else branch, these same fields are added by name with hardcoded string keys like `@"access_token"`, bypassing the sanitization check entirely. **This could be a potential security/validation gap if the `sanitizeResponse` flag is meant to enforce stricter output.**

---

### 3. **OAuth2 Constants and Their String Mappings**

All constants are defined in `MSIDOAuth2Constants.m`. Here are the key ones used in the sanitization function:

| Constant | String Value |
|----------|--------------|
| `MSID_OAUTH2_ACCESS_TOKEN` | `@"access_token"` |
| `MSID_OAUTH2_ID_TOKEN` | `@"id_token"` |
| `MSID_OAUTH2_TOKEN_TYPE` | `@"token_type"` |
| `MSID_OAUTH2_EXPIRES_IN` | `@"expires_in"` |
| `MSID_OAUTH2_EXPIRES_ON` | `@"expires_on"` |
| `MSID_OAUTH2_SCOPE` | `@"scope"` |
| `MSID_OAUTH2_REQUEST_CONFIRMATION` | `@"req_cnf"` |

✅ **Verification:** The constants map to the expected OAuth2 string keys as per the OAuth2 specification.

Now I have a thorough understanding of the change. Let me report my findings:

Now I'll report my findings. Let me use the report_finding format specified:

Here are my review findings for PR #1947:

**Finding 1 — Cached-token path ignores sanitization flag (🟡 Concern)**

In `jsonDictionary`, the `else` branch (line ~179) that builds the response from a cached `MSIDAccessToken` never consults the `sanitizeResponse` boolean. The flag is read at line 130 but only used in the two `tokenResponse`-present branches. While the manual construction currently only emits allow-listed fields by design, the sanitization intent isn't enforced — if a future change adds a field to this `else` branch, it silently bypasses the flight gate. This also creates a semantic inconsistency: reading the flight at the top implies all paths respect it.

**File:** `IdentityCore/src/broker_operation/response/browser_native_message_response/MSIDBrowserNativeMessageGetTokenResponse.m`  
**Line:** 179  
**Confidence:** high  
**Action:** fix — add a code comment documenting why the else branch is inherently safe (it only emits known contract fields), or restructure so the sanitization function is consistently applied.

**Finding 2 — No test for cached-access-token path with flight enabled (🟢 Suggestion)**

All flight-enabled tests route through branches where `tokenResponse` is non-nil. The cached-access-token code path (where `tokenResponse` is nil, `accessToken` is present — exercised by `testJsonDictionary_whenAccessTokenComesFromCache_shouldBuildResponseFromResult`) is only tested with the flight implicitly off. Adding a flight-enabled variant of this test would document the expected behavior and guard against regressions if the cached path is later modified.

**File:** `IdentityCore/tests/MSIDBrowserNativeMessageGetTokenResponseTests.m`  
**Line:** null  
**Confidence:** high

**Finding 3 — No diagnostic logging when sanitization strips fields (🟢 Suggestion)**

When the flight is enabled and `MSIDBrowserNativeMessageGetTokenResponseSanitizedDictionary()` filters the token response, there is no log entry indicating sanitization was active. If a browser caller breaks because a field they depended on was removed, diagnosing the issue would require code-level investigation. A single debug-level log like `MSID_LOG_WITH_CTX(MSIDLogLevelVerbose, nil, @"Browser GetToken response sanitization enabled")` (no token/PII values) would aid production debugging.

**File:** `IdentityCore/src/broker_operation/response/browser_native_message_response/MSIDBrowserNativeMessageGetTokenResponse.m`  
**Line:** 130  
**Confidence:** medium

**Finding 4 — Flight-disabled test doesn't verify sensitive fields pass through (🟢 Suggestion)**

`testJsonDictionary_whenResponseSanitizationFlightDisabled_shouldPreserveLegacyPassthrough` only asserts a single arbitrary key (`future_server_field`). Including sensitive fields like `refresh_token`, `client_info`, and `foci` in the mock response and asserting they ARE present when the flight is OFF would make this a stronger regression test — proving the flight-off path preserves the exact legacy behavior the bug describes.

**File:** `IdentityCore/tests/MSIDBrowserNativeMessageGetTokenResponseTests.m`  
**Line:** null  
**Confidence:** high

**Finding 5 — Legacy `operationTokenResponse` path has no explicit flight-disabled test (ℹ️ Observation)**

The `testJsonDictionary_whenInitializedWithLegacyTokenResponse_shouldPreserveLegacyShape` test doesn't set up a flight provider, so the flight is implicitly OFF. While functionally equivalent, there's no test that explicitly sets the flight to `@NO` for this code path (like the tokenResult tests do). This means the tests don't prove that explicit `@NO` behaves identically to absent for the legacy initializer path.

**File:** `IdentityCore/tests/MSIDBrowserNativeMessageGetTokenResponseTests.m`  
**Line:** null  
**Confidence:** medium

---

**Summary:** The core sanitization logic is sound — the allow-list approach is correct and the security fix is well-structured. The main concern is that the cached-access-token code path doesn't enforce the sanitization flag, creating an inconsistency that could lead to future regressions. Test coverage is good but has gaps around the cached-token path with flight enabled and explicit sensitive-field passthrough verification.