Let me use the GitHub MCP tools directly to get the PR details and changed files.

Now let me examine the full implementation file and check for any PII-related concerns:

Now let me verify the test file for any PII usage:

I now have a thorough understanding of this PR. Let me summarize my privacy review findings.

**Privacy Review Summary — PR #1947: Sanitize browser native GetToken responses**

This PR is a **privacy improvement** that fixes a CWE-201 information disclosure issue. It adds a flight-gated allow-list sanitization to prevent leaking broker-internal fields (refresh tokens, `client_info`, FoCI, ADI, provider metadata, and arbitrary server fields) through browser native GetToken responses.

**Privacy assessment — No violations found:**

1. **Data minimization** ✅ — Uses an explicit allow-list approach (not deny-list). Only `access_token`, `id_token`, `token_type`, `expires_in`, `expires_on`, `scope`, and `req_cnf` pass through when enabled. This is textbook data minimization.

2. **PII in logs** ✅ — All log messages are generic error strings ("Failed to create token json response.") with no PII. No token values, account identifiers, or personal data logged.

3. **No new PII exposure** ✅ — The `account` and `properties.UPN` blocks were already part of the existing browser contract and remain unchanged. No new personal data fields are introduced.

4. **Test data** ✅ — Tests use synthetic values (`synthetic-access-token`, `synthetic-id-token`) and `@contoso.com` (Microsoft's standard test domain). No production PII in test fixtures.

5. **Consent/retention/deletion** — Not applicable. This change restricts outbound data exposure; it does not collect, store, or process new personal data.

The disabled-by-default flight means the legacy exposure continues until ECS enables it, but this is an explicit staged rollout decision documented in the PR — not a privacy gap.

**No privacy findings to report.** This PR improves the privacy posture of the codebase by applying data minimization to an information disclosure vulnerability.