Now let me examine the full source file to understand all code paths:

Let me verify the OAuth2 constants used in the allowlist and check for any missing imports:

I've reviewed all four changed source files in this PR. The change introduces a flight-gated sanitization of browser GetToken responses, filtering the token response dictionary to an allowlist of 7 OAuth2 keys.

**Analysis summary:**

1. **`MSIDBrowserNativeMessageGetTokenResponseSanitizedDictionary`** — Correctly iterates an allowlist, copies only present values. Called only after nil-guards on `tokenResponseJson`, so no nil input risk.

2. **`jsonDictionary` — `operationTokenResponse` path (line 132):** Flight check at line 130, sanitization applied at line 142. Account/state/properties are added *after* sanitization — correct, as these are structural metadata, not server token fields.

3. **`jsonDictionary` — `tokenResult` path with `tokenResponse` (line 166):** Same pattern, correctly applied at line 175.

4. **`jsonDictionary` — cached path without `tokenResponse` (line 179-208):** No sanitization applied here, which is **correct** — this path manually constructs the response from cached values and only includes fields (`access_token`, `token_type`, `id_token`, `scope`, `expires_on`, `expires_in`) that are all in the allowlist. No server fields can leak through.

5. **Flight default behavior:** `boolForKey:` returns `NO` when no provider is set or key is absent (verified in `MSIDFlightManager.m:140`), so the flight defaults to OFF — preserving legacy passthrough behavior.

6. **All 7 OAuth2 constants** in the allowlist verified to exist in `MSIDOAuth2Constants.h/.m`.

7. **Tests** cover all paths: flight ON, flight OFF, flight absent, nil response, both initializer paths, and the cached path with sanitization enabled.

**No correctness bugs found.** The implementation is clean — the sanitization function is straightforward, the flight gating defaults safely to OFF, and all code paths are properly covered.